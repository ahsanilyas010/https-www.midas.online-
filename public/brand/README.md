# Brand assets

CallMilalo's logo is drawn in code (`src/components/brand/mark.tsx`, and
`src/app/icon.tsx` for the favicon). To use a supplied logo instead, drop
the files here and swap `<BrandMark />` for an `<img>` pointing at them.
